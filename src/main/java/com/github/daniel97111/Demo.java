package com.github.daniel97111;

import com.github.daniel97111.controller.FileSafeController;
import com.github.daniel97111.model.PasswordEntry;
import com.github.daniel97111.model.PasswordSafe;

import java.util.Arrays;


public class Demo {
/*    public static void main(String[] args) {
        PasswordSafe safe = new PasswordSafe();

        PasswordEntry entry = new PasswordEntry(0, "O2.pl", "haslo", "login12");
        PasswordEntry entry2 = new PasswordEntry(1, "Facebook", "18978WoW", "Daniel97");
        PasswordEntry entry3 = new PasswordEntry(1, "interia", "18978WoW", "Daniel97");
        PasswordEntry entry4 = new PasswordEntry(1, "sexfotka", "18978WoW", "Daniel97");

        safe.addEntries(entry.getService(), entry.getLogin(), entry.getPassword());
        safe.addEntries(entry2.getService(), entry2.getLogin(), entry2.getPassword());

        //safe.removeEntries(entry.getId());

        System.out.println(safe.show("O2.pl"));
        System.out.println(safe.show("Facebook"));


        FileSafeController fileSafe = new FileSafeController();

        fileSafe.saveToFile(Arrays.asList(entry, entry2, entry3, entry4, entry2));

        System.out.println(fileSafe.readFromFile("new-pass-keeper-file.pwm"));
    }*/
}
